function testing() {
    const [data, setData] = useState(null);


async getData= ()=>{
const near = await ccnnectNear();

      //========Write code here=========
      const response = await near.connection.provider.block({
        finality: "final",
      });
      setData(response);
      //==============================
}

    useEffect( async()=>{
       getData();

       //========check Tx Status=========
            //const response = await near.connection.provider.txStatus("H7vHN2dMs6JCJnXSNBXBW5B5Xo1gJkVtEM7EG8Q5UqGn","tbt.testnet");
            //setData(response);

      //============================
    },[])

    return <ViewResponse response={data}/>
  }
